import { darkColors } from '../theme/themes';
import type { ThemeColors } from '../theme/themes';
export { spacing, fontSizes, radius } from './layout';

export const colors: ThemeColors = darkColors;
export type { ThemeColors };
